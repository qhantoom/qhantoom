// the exit code for failure
pub const EXIT_FAILURE: i32 = 1;

// the compiler output directory
pub const PATH_DIRECTORY: &str = "program";

// the entry point of the program
pub const PROGRAM_ENTRY: &str = "main";
